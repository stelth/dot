[user]
    name = Jason Cox
    email = Jason.P.Cox@ibm.com

[merge]
    conflicttyle = diff3
    renamelimit = 999999
    tool = nvimdiff

[mergetool "nvimdiff"]
    prompt = false
    cmd = ~/bin/nvim -d $LOCAL $REMOTE $MERGED

[status]
    showUntrackedFiles = all

[push]
    default = simple
    followtags = true

[pull]
    rebase = false

[ghq]
    root = ~/repos/

[diff]
    tool = nvimdiff

[difftool "nvimdiff"]
    prompt = false
    cmd = ~/bin/nvim -d $LOCAL $REMOTE
